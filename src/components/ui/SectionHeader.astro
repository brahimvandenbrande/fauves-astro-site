---
interface Props {
  title?: string;
  level?: 1 | 2 | 3 | 4 | 5 | 6;
  class?: string;
  align?: 'left' | 'center' | 'right';
  size?: 'sm' | 'md' | 'lg' | 'xl';
  id?: string;
}

const {
  title = '',
  level = 2,
  class: className = '',
  align = 'left',
  size = 'lg',
  id,
} = Astro.props;

const alignClasses = {
  left: 'text-left',
  center: 'text-center',
  right: 'text-right',
};

const sizeClasses = {
  sm: 'text-2xl md:text-3xl',
  md: 'text-3xl md:text-4xl',
  lg: 'text-4xl md:text-5xl',
  xl: 'text-5xl md:text-6xl',
};

const HeadingTag = `h${level}` as const;
---

<HeadingTag 
  id={id || `section-heading-${Math.random().toString(36).substr(2, 9)}`}
  class:list={[
    'font-degular font-light leading-tight text-black gsap-heading',
    sizeClasses[size],
    alignClasses[align],
    className,
    'opacity-0' // Start with 0 opacity, GSAP will handle the animation
  ]}
  data-gsap-animate="true"
>
  <slot>{title}</slot>
</HeadingTag>
